---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: flask-ingress
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: "jasmineeds-flask.coe332.tacc.cloud"
    http:
        paths:
        - pathType: Prefix
          path: "/"
          backend:
            service:
              name: hello-flask-nodeport-service
              port:
                  number: 5000
